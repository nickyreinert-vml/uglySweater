Purpose:
- Store deployment and runtime configuration artifacts.
Main Files:
- deploy.yml for remote deployment.
- config.json for application level defaults.
Sub Folders:
- None beyond files already present.
Dependent Folders:
- functions/data/config_loader reads config.json.
